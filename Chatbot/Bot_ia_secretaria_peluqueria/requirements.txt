# Requisitos pinneados para producción (compatibles con tu código actual)
Flask~=3.0
gunicorn~=21.2
python-dotenv~=1.0

# OpenAI SDK v1 (tu código usa openai.chat.completions.create)
openai>=1.14,<2

# Parsing fechas en español
dateparser~=1.2

# Configuración con Pydantic v2
pydantic>=2.5,<3
pydantic-settings>=2.2,<3

# ORM y MySQL
SQLAlchemy>=2.0,<3
pymysql>=1.1,<2

# Google Calendar API
google-api-python-client>=2.120,<3
google-auth>=2.28,<3
google-auth-oauthlib>=1.2,<2
google-auth-httplib2>=0.2,<1

# Rate limiting + Redis
Flask-Limiter[redis]>=3.5,<4
redis>=5.0,<6

# Reintentos
tenacity>=8.2,<9

sentry-sdk[Flask]>=1.45,<2

alembic
pytest
pytest-cov
phonenumbers
